import { StyledParagraph } from "./DefaultOrganism.style";

const Default = ({children}:any) => {
    return (
        <StyledParagraph>
            {children}
        </StyledParagraph>
    );
}

export default Default;