import { StyledParagraph } from "./Default.style";

const Default = ({children}:any) => {
    return (
        <StyledParagraph>
            {children}
        </StyledParagraph>
    );
}

export default Default;